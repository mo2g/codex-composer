# Project Bootstrapped With Codex App Template

This repository was initialized with the Codex App Template.

## What To Do Next

1. Replace this README with a project-specific overview.
2. Keep `AGENTS.md` short and accurate for this repository.
3. Use `docs/codex-quickstart.md` for the installed workflow map, then `docs/codex-task-card-workflow.md` and `docs/codex-debug-workflow.md` for detailed rules.
4. Add `.codex/config.toml` only if repo-owned defaults or verification hints are actually worth keeping.
5. Keep only the docs, skills, and `docs/_codex/` artifacts this repository wants to maintain.
